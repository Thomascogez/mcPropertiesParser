{
  "name": "@thomascogez/mcprotertiesparser",
  "version": "1.0.0",
  "description": "A simple nodejs module to simply parse and edit minecraft server config",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Thomascogez",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "clean": "rimraf dist/*",
    "build": "npm run clean && tsc"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.8",
    "jest": "^26.5.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  }
}
